import Tempo from "../Tempo";
import {currentTempo} from "../index";
export default class Music extends Tempo{
    // public readonly startingTimeCode: number
    // constructor(startingTimeCode: number) {
    //     super(currentTempo.getBpm(), currentTempo.getLoopLength());
    //     this.startingTimeCode = musicTimeCode;
    // }
    //
}